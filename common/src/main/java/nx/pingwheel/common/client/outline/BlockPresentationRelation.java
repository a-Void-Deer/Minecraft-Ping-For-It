package nx.pingwheel.common.client.outline;

/** Describes how a render subject relates to its captured block target. */
public enum BlockPresentationRelation {
	DIRECT,
	COMPOSITE,
	PROXY_TO_OWNER
}
